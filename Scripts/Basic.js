function jqCssToggle() {
    $(selector).on(events, function () {
        $(selector).css(data) == data ?
            $(selector).css(data, data) :
            $(selector).css(data, data);
    });
}

$(selector).one(events, data, function (e) {
    
});